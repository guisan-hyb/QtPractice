

# **Qt 事件处理流程笔记**

#### **事件创建**

- **描述**：当用户与应用程序交互（如鼠标点击、键盘输入）或系统内部触发（例如定时器事件、自定义事件）时，都会生成一个事件。
- **进入事件循环**：这些事件由操作系统或 Qt 内部生成，并进入 Qt 的事件循环。
- **目标对象 X 和父节点 Y**：
- 目标对象记为 `X`，它是 `QObject` 或其子类实例。
- `X` 在 Qt 对象树中的父节点记为 `Y`。
- **注意**：并非所有事件都支持父子冒泡，例如某些特殊的事件（如 `QPaintEvent`）只在当前对象中处理。

#### **事件过滤器（Event Filter）**

- **安装和处理**：如果目标对象 `X` 或其上层对象安装了事件过滤器（通过 `installEventFilter()` 方法），则事件首先传递给过滤器处理。
- **返回值的作用**：
- 返回 `true`：表示过滤器已处理该事件，事件传播立即终止，不会再传递给 `X` 的 `event()` 函数。
- 返回 `false`：表示未处理该事件，允许事件继续传递到 `X` 的 `event()` 函数。
- **事件状态标记（**`**event->accept()**` **/** `**event->ignore()**`**）**：
- 在过滤器中调用 `accept()` 或 `ignore()` 仅是修改事件内部的“已接受状态”，不会直接影响传播结果。
- 传播行为完全由过滤器返回的布尔值控制。

#### **调用对象** `**X**` **的** `**event()**` **函数**

- **事件传递**：当事件未被过滤器截断（过滤器返回 `false`）后，事件进入 `X` 的 `event()` 函数。
- **工作流程**：
- `event()` 是所有事件的入口点，根据事件类型调用相应的具体事件处理函数（如 `keyPressEvent()`、`mousePressEvent()`）。
- **返回值的作用**：
- 返回 `true`：事件被视为已处理，传播终止；
- 返回 `false`：事件未被完全处理，允许事件继续传递给父对象 `Y` 的事件过滤器（适用于支持冒泡的事件类型）。
- **调用** `**accept()**` **与** `**ignore()**`：
- 在 `event()` 函数中调用 `event->accept()` 或 `event->ignore()` 会修改事件状态，但最终传播决定权在于 `event()` 的返回值。
- 即使在 `event()` 中调用了 `event->ignore()`，只要返回值为 `true`，事件也不会继续冒泡。

#### **具体的事件处理函数**

- **描述**：具体事件处理函数（如 `keyPressEvent()` 或 `mousePressEvent()`）通常没有返回值，它们主要负责处理特定类型的事件。
- **调用** `**accept()**` **和** `**ignore()**`：
- 可在函数内调用 `event->accept()` 标记事件为已处理，或调用 `event->ignore()` 标记事件未处理。
- **默认行为**：
- 不同事件类型和控件的默认实现可能对事件状态做出不同处理（例如某些 `QWidget` 默认对键盘事件调用 `ignore()` 以便让事件继续传递）。

#### **事件传递到父对象** `**Y**`**（事件冒泡）**

- **条件**：如果对象 `X` 在 `event()` 中未处理该事件（即返回 `false` 或调用了 `event->ignore()`），且该事件类型支持冒泡，事件会传递给父对象 `Y` 的事件过滤器。
- **事件过滤器传递**：
- 如果父对象 `Y` 安装了事件过滤器，事件首先传递给这些过滤器。
- 如果事件过滤器返回 `true`，事件传播终止；如果返回 `false`，事件继续传递给父对象 `Y` 的 `event()` 函数。
- **传递到** `**event()**` **函数**：
- 如果父对象 `Y` 没有安装事件过滤器，或者事件过滤器返回 `false`，事件将传递给父对象 `Y` 的 `event()` 函数。
- **注意**：
- 对于 `QWidget` 类型的对象，事件传递通常遵循其父组件（通过 `parentWidget()` 获取）；
- 普通 `QObject` 对象的事件一般不会自动冒泡；
- 冒泡传递适用于特定事件（如鼠标、键盘事件），并非所有事件都执行这一过程。

### **关键总结**

1. **事件创建**：涉及操作系统事件、Qt 内部事件、定时器与自定义事件等，目标对象为 `X`。
2. **事件过滤器**：通过返回值（`true/false`）决定是否拦截事件；内部调用 `accept()/ignore()` 仅改变状态，不影响传播决策。
3. **事件处理**：`X::event()` 根据事件类型调用具体处理函数，最终返回值决定事件是否处理完成。
4. **事件冒泡**：只有未处理的、支持冒泡的事件才会传递给父对象 `Y` 的事件过滤器；然后才传递给父对象的 `event()` 函数；并且 `QWidget` 的父组件与 `QObject` 父节点存在区别。





#### Q:既然传播决策由返回值决定，那accept()和ignore()方法改变状态又有什么用？ 

这是一个极其深刻且直击 Qt 事件系统核心的问题！

你的笔记非常精准地抓住了 `event()` 和事件过滤器的 **返回值（bool）** 才是决定事件是否继续传播的“最终判决书”。那么，既然返回值说了算，`accept()` 和 `ignore()` 改变内部状态到底有什么用？

一句话回答：**`accept()` 和 `ignore()` 是具体事件处理函数（如 `mousePressEvent`）与 `event()` 函数之间沟通的“内部信使”，它们共同决定了 `event()` 最终该返回什么值。**

我们来剥丝抽茧地看这背后的逻辑。



### 一、 核心矛盾：具体事件处理函数没有返回值！

注意你的笔记中有一条：“具体事件处理函数（如 `keyPressEvent()`）通常**没有返回值**（void）”。

这就是问题的根源！

- `event(QEvent *e)` 有返回值（`bool`），它能直接告诉 Qt 系统-event_loop：“我处理完了，别往下传了（return true）” 或 “我处理不了，交给我爹吧（return false）”。
- 但是 `mousePressEvent(QMouseEvent *e)` 是 `void`！它无法通过返回值向 `event()` 汇报工作。

**那么，当 `event()` 内部调用了 `mousePressEvent()` 后，`event()` 怎么知道该返回 true 还是 false 呢？**

答案就是：**通过检查事件的状态标记，也就是 `e->isAccepted()`。**



### 二、 Qt 内部的默认协作逻辑

Qt 框架中 `QWidget::event()` 的底层源码逻辑，简化后大致是这样的：

```cpp
bool QWidget::event(QEvent *e) 
{
    switch (e->type()) {
        case QEvent::MouseButtonPress:
            // 调用具体的处理函数
            this->mousePressEvent(static_cast<QMouseEvent*>(e));
            
            // 关键来了！event() 根据具体函数修改后的事件状态，决定自己的返回值
            return e->isAccepted(); 
            
        // ... 其他事件类型
    }
    return QObject::event(e);
}
```

**这就把返回值和状态标记完美打通了：**

1. 当你在 `mousePressEvent` 中调用 `event->accept()` 时，你把状态改成了“已接受”。
2. `mousePressEvent` 执行完毕，控制权回到 `event()`。
3. `event()` 检查 `e->isAccepted()`，发现是 `true`，于是 `event()` **返回 true**。
4. Qt 事件系统看到返回 `true`，停止传播。

反之，如果你在 `mousePressEvent` 中调用 `event->ignore()`：

1. 状态变成“未接受”。
2. `event()` 检查发现是 `false`，于是 `event()` **返回 false**。
3. Qt 事件系统看到返回 `false`，事件冒泡给父组件。

**结论：在具体事件处理函数中，`accept()/ignore()` 的作用就是间接地控制 `event()` 的返回值！**



### 三、 继承链中的“接力棒”

`accept()/ignore()` 还有一个极其重要的应用场景：**在基类和派生类的调用链中传递意图。**

假设你重写了 `mousePressEvent`，但只想处理左键，右键希望交给基类（或者父组件）处理：

```cpp
void MyWidget::mousePressEvent(QMouseEvent *event) 
{
    if (event->button() == Qt::LeftButton) {
        // 我处理了左键
        qDebug() << "左键被按下";
        event->accept(); // 标记已处理，这样 event() 就会返回 true
    } else {
        // 我不处理右键，交给基类去处理
        QWidget::mousePressEvent(event); 
        // 注意：基类的默认实现通常会调用 event->ignore()
        // 所以 event 出来后状态是 ignore，最终 event() 返回 false，事件冒泡
    }
}
```

如果你不调用 `accept()` 也不调用 `ignore()` 呢？

Qt 有默认规则：**事件刚创建时，默认状态是 `accept`（已接受）**。但是，`QWidget` 的默认事件处理函数（如 `QWidget::mousePressEvent`）内部会默认调用 `event->ignore()`。所以不写任何处理，直接调用基类，事件通常会冒泡。



### 四、 为什么 event() 中可以直接返回 bool，还要设计 accept/ignore？

你可能会想，既然如此，全用返回值不就行了？为什么还要搞一套状态标记？

因为**灵活性**和**解耦**。

1. **允许在 event() 中强行扭转乾坤**： 就像你笔记里写的：“即使在 event() 中调用了 event->ignore()，只要返回值为 true，事件也不会继续冒泡。” 这赋予了 `event()` 绝对的权威。`event()` 是总调度，它可以根据全局情况，无视底层具体函数（如 `mousePressEvent`）传上来的 `ignore()` 状态，强行拦截事件。

   ```cpp
   bool MyWidget::event(QEvent *e) {
       if (e->type() == QEvent::MouseButtonPress) {
           this->mousePressEvent(static_cast<QMouseEvent*>(e));
           // 就算 mousePressEvent 里面 ignore 了，我作为总管也要强行截断！
           return true; 
       }
       return QWidget::event(e);
   }
   ```

2. **跨函数的状态持久化**： 事件是一个对象，它在不同的函数之间传递。状态标记附着在对象身上，无论这个事件被传给哪个函数，大家都能读取它之前被处理的状态。而函数的返回值一旦返回，就消失了。



### 总结：打个比方

- **事件系统**：快递员（只看返回值：true=签收终结，false=拒收转交）。
- **`event()` 函数**：公司前台。前台有权直接把快递签收或拒收，也可以交给具体的部门处理。
- **具体函数 (`mousePressEvent`)**：技术部。技术部没有权利直接跟快递员说话（void 无返回值）。
- **`accept()/ignore()`**：技术部在快递单上盖的章。

**流程**： 快递到前台（`event`） -> 前台交给技术部（`mousePressEvent`） -> 技术部处理完，在快递单上盖章（`accept` 或 `ignore`） -> 快递单交回前台 -> **前台看一眼快递单上的章，然后决定跟快递员说“签收”还是“拒收”。**

所以，`accept()/ignore()` 不是没用的，它是底层具体业务逻辑向上层调度中心汇报意图的**唯一通道**。